apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enav-service-architecture.fullname" . }}-test-connection"
  labels:
    {{- include "enav-service-architecture.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enav-service-architecture.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
